package frc.robot.generated;

import org.wpilib.math.autodiff.Variable;

/**
 * Generated by `just tune_turret` - edits will be overwritten!  
 * Contains data tuned by `misc/turretTuning/analyze_data.py`.
 * Generated equations are as follows:
 * - FlywheelCurrentModel: y = 4.50954981 + 0.03933206 * flywheel_vel + -0.17261779 * azimuth_vel + -0.01129331 * hood_vel + -0.00003148 * flywheel_vel^2 + 0.00049094 * flywheel_vel * azimuth_vel + 0.00001568 * flywheel_vel * hood_vel + 0.00037112 * azimuth_vel^2 + 0.00000077 * azimuth_vel * hood_vel + 0.00001883 * hood_vel^2
 * - AzimuthCurrentModel: y = 3.13158136 + 0.01567187 * flywheel_vel + 0.56797553 * azimuth_vel + -0.03258733 * hood_vel + -0.00002328 * flywheel_vel^2 + 0.00038505 * flywheel_vel * azimuth_vel + -0.00000128 * flywheel_vel * hood_vel + -0.01541767 * azimuth_vel^2 + -0.00017302 * azimuth_vel * hood_vel + 0.00005798 * hood_vel^2
 * - HoodCurrentModel: y = 7.49297619 + 0.00385578 * flywheel_vel + 0.17269444 * azimuth_vel + 0.11114643 * hood_vel + -0.00002058 * flywheel_vel^2 + -0.00018575 * flywheel_vel * azimuth_vel + 0.00001628 * flywheel_vel * hood_vel + 0.00064642 * azimuth_vel^2 + -0.00050922 * azimuth_vel * hood_vel + -0.00016015 * hood_vel^2
 * - TotalCurrentModel: y = 15.13410736 + 0.05885972 * flywheel_vel + 0.56805218 * azimuth_vel + 0.06726579 * hood_vel + -0.00007534 * flywheel_vel^2 + 0.00069024 * flywheel_vel * azimuth_vel + 0.00003069 * flywheel_vel * hood_vel + -0.01440013 * azimuth_vel^2 + -0.00068148 * azimuth_vel * hood_vel + -0.00008335 * hood_vel^2
 */
public class TurretTuningData {
    /**
     * All input velocities are in motor radians per second.
     * y = 4.50954981 + 0.03933206 * flywheel_vel + -0.17261779 * azimuth_vel + -0.01129331 * hood_vel + -0.00003148 * flywheel_vel^2 + 0.00049094 * flywheel_vel * azimuth_vel + 0.00001568 * flywheel_vel * hood_vel + 0.00037112 * azimuth_vel^2 + 0.00000077 * azimuth_vel * hood_vel + 0.00001883 * hood_vel^2
     */
    public static final class FlywheelCurrentModel {
        public static final double bias = 4.50954981;
        private static final Variable bias_var = new Variable(bias);
        public static final double flywheel = 0.03933206;
        private static final Variable flywheel_var = new Variable(flywheel);
        public static final double azimuth = -0.17261779;
        private static final Variable azimuth_var = new Variable(azimuth);
        public static final double hood = -0.01129331;
        private static final Variable hood_var = new Variable(hood);
        public static final double flywheel2 = -0.00003148;
        private static final Variable flywheel2_var = new Variable(flywheel2);
        public static final double flywheel_azimuth = 0.00049094;
        private static final Variable flywheel_azimuth_var = new Variable(flywheel_azimuth);
        public static final double flywheel_hood = 0.00001568;
        private static final Variable flywheel_hood_var = new Variable(flywheel_hood);
        public static final double azimuth2 = 0.00037112;
        private static final Variable azimuth2_var = new Variable(azimuth2);
        public static final double azimuth_hood = 0.00000077;
        private static final Variable azimuth_hood_var = new Variable(azimuth_hood);
        public static final double hood2 = 0.00001883;
        private static final Variable hood2_var = new Variable(hood2);

        public static double calculate(double flywheel_vel, double azimuth_vel, double hood_vel) {
            return bias + flywheel * flywheel_vel + azimuth * azimuth_vel + hood * hood_vel + flywheel2 * Math.pow(flywheel_vel, 2) + flywheel_azimuth * flywheel_vel * azimuth_vel + flywheel_hood * flywheel_vel * hood_vel + azimuth2 * Math.pow(azimuth_vel, 2) + azimuth_hood * azimuth_vel * hood_vel + hood2 * Math.pow(hood_vel, 2);
        }

        public static Variable calculate(Variable flywheel_vel, Variable azimuth_vel, Variable hood_vel) {
            return bias_var.plus(flywheel_var.times(flywheel_vel)).plus(azimuth_var.times(azimuth_vel)).plus(hood_var.times(hood_vel)).plus(flywheel2_var.times(flywheel_vel).times(flywheel_vel)).plus(flywheel_azimuth_var.times(flywheel_vel).times(azimuth_vel)).plus(flywheel_hood_var.times(flywheel_vel).times(hood_vel)).plus(azimuth2_var.times(azimuth_vel).times(azimuth_vel)).plus(azimuth_hood_var.times(azimuth_vel).times(hood_vel)).plus(hood2_var.times(hood_vel).times(hood_vel));
        }
    }
    /**
     * All input velocities are in motor radians per second.
     * y = 3.13158136 + 0.01567187 * flywheel_vel + 0.56797553 * azimuth_vel + -0.03258733 * hood_vel + -0.00002328 * flywheel_vel^2 + 0.00038505 * flywheel_vel * azimuth_vel + -0.00000128 * flywheel_vel * hood_vel + -0.01541767 * azimuth_vel^2 + -0.00017302 * azimuth_vel * hood_vel + 0.00005798 * hood_vel^2
     */
    public static final class AzimuthCurrentModel {
        public static final double bias = 3.13158136;
        private static final Variable bias_var = new Variable(bias);
        public static final double flywheel = 0.01567187;
        private static final Variable flywheel_var = new Variable(flywheel);
        public static final double azimuth = 0.56797553;
        private static final Variable azimuth_var = new Variable(azimuth);
        public static final double hood = -0.03258733;
        private static final Variable hood_var = new Variable(hood);
        public static final double flywheel2 = -0.00002328;
        private static final Variable flywheel2_var = new Variable(flywheel2);
        public static final double flywheel_azimuth = 0.00038505;
        private static final Variable flywheel_azimuth_var = new Variable(flywheel_azimuth);
        public static final double flywheel_hood = -0.00000128;
        private static final Variable flywheel_hood_var = new Variable(flywheel_hood);
        public static final double azimuth2 = -0.01541767;
        private static final Variable azimuth2_var = new Variable(azimuth2);
        public static final double azimuth_hood = -0.00017302;
        private static final Variable azimuth_hood_var = new Variable(azimuth_hood);
        public static final double hood2 = 0.00005798;
        private static final Variable hood2_var = new Variable(hood2);

        public static double calculate(double flywheel_vel, double azimuth_vel, double hood_vel) {
            return bias + flywheel * flywheel_vel + azimuth * azimuth_vel + hood * hood_vel + flywheel2 * Math.pow(flywheel_vel, 2) + flywheel_azimuth * flywheel_vel * azimuth_vel + flywheel_hood * flywheel_vel * hood_vel + azimuth2 * Math.pow(azimuth_vel, 2) + azimuth_hood * azimuth_vel * hood_vel + hood2 * Math.pow(hood_vel, 2);
        }

        public static Variable calculate(Variable flywheel_vel, Variable azimuth_vel, Variable hood_vel) {
            return bias_var.plus(flywheel_var.times(flywheel_vel)).plus(azimuth_var.times(azimuth_vel)).plus(hood_var.times(hood_vel)).plus(flywheel2_var.times(flywheel_vel).times(flywheel_vel)).plus(flywheel_azimuth_var.times(flywheel_vel).times(azimuth_vel)).plus(flywheel_hood_var.times(flywheel_vel).times(hood_vel)).plus(azimuth2_var.times(azimuth_vel).times(azimuth_vel)).plus(azimuth_hood_var.times(azimuth_vel).times(hood_vel)).plus(hood2_var.times(hood_vel).times(hood_vel));
        }
    }
    /**
     * All input velocities are in motor radians per second.
     * y = 7.49297619 + 0.00385578 * flywheel_vel + 0.17269444 * azimuth_vel + 0.11114643 * hood_vel + -0.00002058 * flywheel_vel^2 + -0.00018575 * flywheel_vel * azimuth_vel + 0.00001628 * flywheel_vel * hood_vel + 0.00064642 * azimuth_vel^2 + -0.00050922 * azimuth_vel * hood_vel + -0.00016015 * hood_vel^2
     */
    public static final class HoodCurrentModel {
        public static final double bias = 7.49297619;
        private static final Variable bias_var = new Variable(bias);
        public static final double flywheel = 0.00385578;
        private static final Variable flywheel_var = new Variable(flywheel);
        public static final double azimuth = 0.17269444;
        private static final Variable azimuth_var = new Variable(azimuth);
        public static final double hood = 0.11114643;
        private static final Variable hood_var = new Variable(hood);
        public static final double flywheel2 = -0.00002058;
        private static final Variable flywheel2_var = new Variable(flywheel2);
        public static final double flywheel_azimuth = -0.00018575;
        private static final Variable flywheel_azimuth_var = new Variable(flywheel_azimuth);
        public static final double flywheel_hood = 0.00001628;
        private static final Variable flywheel_hood_var = new Variable(flywheel_hood);
        public static final double azimuth2 = 0.00064642;
        private static final Variable azimuth2_var = new Variable(azimuth2);
        public static final double azimuth_hood = -0.00050922;
        private static final Variable azimuth_hood_var = new Variable(azimuth_hood);
        public static final double hood2 = -0.00016015;
        private static final Variable hood2_var = new Variable(hood2);

        public static double calculate(double flywheel_vel, double azimuth_vel, double hood_vel) {
            return bias + flywheel * flywheel_vel + azimuth * azimuth_vel + hood * hood_vel + flywheel2 * Math.pow(flywheel_vel, 2) + flywheel_azimuth * flywheel_vel * azimuth_vel + flywheel_hood * flywheel_vel * hood_vel + azimuth2 * Math.pow(azimuth_vel, 2) + azimuth_hood * azimuth_vel * hood_vel + hood2 * Math.pow(hood_vel, 2);
        }

        public static Variable calculate(Variable flywheel_vel, Variable azimuth_vel, Variable hood_vel) {
            return bias_var.plus(flywheel_var.times(flywheel_vel)).plus(azimuth_var.times(azimuth_vel)).plus(hood_var.times(hood_vel)).plus(flywheel2_var.times(flywheel_vel).times(flywheel_vel)).plus(flywheel_azimuth_var.times(flywheel_vel).times(azimuth_vel)).plus(flywheel_hood_var.times(flywheel_vel).times(hood_vel)).plus(azimuth2_var.times(azimuth_vel).times(azimuth_vel)).plus(azimuth_hood_var.times(azimuth_vel).times(hood_vel)).plus(hood2_var.times(hood_vel).times(hood_vel));
        }
    }
    /**
     * All input velocities are in motor radians per second.
     * y = 15.13410736 + 0.05885972 * flywheel_vel + 0.56805218 * azimuth_vel + 0.06726579 * hood_vel + -0.00007534 * flywheel_vel^2 + 0.00069024 * flywheel_vel * azimuth_vel + 0.00003069 * flywheel_vel * hood_vel + -0.01440013 * azimuth_vel^2 + -0.00068148 * azimuth_vel * hood_vel + -0.00008335 * hood_vel^2
     */
    public static final class TotalCurrentModel {
        public static final double bias = 15.13410736;
        private static final Variable bias_var = new Variable(bias);
        public static final double flywheel = 0.05885972;
        private static final Variable flywheel_var = new Variable(flywheel);
        public static final double azimuth = 0.56805218;
        private static final Variable azimuth_var = new Variable(azimuth);
        public static final double hood = 0.06726579;
        private static final Variable hood_var = new Variable(hood);
        public static final double flywheel2 = -0.00007534;
        private static final Variable flywheel2_var = new Variable(flywheel2);
        public static final double flywheel_azimuth = 0.00069024;
        private static final Variable flywheel_azimuth_var = new Variable(flywheel_azimuth);
        public static final double flywheel_hood = 0.00003069;
        private static final Variable flywheel_hood_var = new Variable(flywheel_hood);
        public static final double azimuth2 = -0.01440013;
        private static final Variable azimuth2_var = new Variable(azimuth2);
        public static final double azimuth_hood = -0.00068148;
        private static final Variable azimuth_hood_var = new Variable(azimuth_hood);
        public static final double hood2 = -0.00008335;
        private static final Variable hood2_var = new Variable(hood2);

        public static double calculate(double flywheel_vel, double azimuth_vel, double hood_vel) {
            return bias + flywheel * flywheel_vel + azimuth * azimuth_vel + hood * hood_vel + flywheel2 * Math.pow(flywheel_vel, 2) + flywheel_azimuth * flywheel_vel * azimuth_vel + flywheel_hood * flywheel_vel * hood_vel + azimuth2 * Math.pow(azimuth_vel, 2) + azimuth_hood * azimuth_vel * hood_vel + hood2 * Math.pow(hood_vel, 2);
        }

        public static Variable calculate(Variable flywheel_vel, Variable azimuth_vel, Variable hood_vel) {
            return bias_var.plus(flywheel_var.times(flywheel_vel)).plus(azimuth_var.times(azimuth_vel)).plus(hood_var.times(hood_vel)).plus(flywheel2_var.times(flywheel_vel).times(flywheel_vel)).plus(flywheel_azimuth_var.times(flywheel_vel).times(azimuth_vel)).plus(flywheel_hood_var.times(flywheel_vel).times(hood_vel)).plus(azimuth2_var.times(azimuth_vel).times(azimuth_vel)).plus(azimuth_hood_var.times(azimuth_vel).times(hood_vel)).plus(hood2_var.times(hood_vel).times(hood_vel));
        }
    }
}
